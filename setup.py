from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Template repository with Cookie Cutter Infrastructure that can be forked for every new Data Science project',
    author='aignosi',
    license='MIT',
)
